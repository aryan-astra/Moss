<Project Sdk="Microsoft.NET.Sdk">
  <!-- Reconstructed from the shipped Moss.Core assembly (see README.md,
       "Source provenance"). Original project layout/names preserved
       where recoverable; absolute machine paths replaced with NuGet. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>12.0</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Markdig" Version="0.37.0" />
  </ItemGroup>
</Project>
